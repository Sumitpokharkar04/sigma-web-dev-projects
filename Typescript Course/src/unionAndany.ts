//now let suppose we want to write 1 million then there are a lot of ways like "1M" or 1000000 now in some cases we will need that our 
//variable shall except two or more than two data types in that case we use union.for ex :

let subscriibers : string|number = 1000000

let apirequeststatus : 'pending'|'approved'|'reject'='pending'// we can also make custom types like this

let name : unknown = "jau"
name = 2