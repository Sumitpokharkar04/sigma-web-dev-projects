let drink = 0

drink = 6

let cups = Math.random() > 0.5 ? 10 : 4

let channelName = "chaiaurcode"

//there are two types of types in typescript.

//type inference =>it is a phenomeneon where ts automatically detects a type of variable.
// type annotation => here we explicitly define a type of variable or return type of a function.